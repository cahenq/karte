import 'package:flutter/material.dart';

class TelaComunidade extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Comunidade'),
      ),
      body: Center(
        child: Text('Esta é a tela de comunidade.'),
      ),
    );
  }
}
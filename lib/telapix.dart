import 'package:flutter/material.dart';

class TelaPix extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tela Pix'),
      ),
      body: Center(
        child: Text('Conteúdo da Tela Pix'),
      ),
    );
  }
}

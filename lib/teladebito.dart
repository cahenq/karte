import 'package:flutter/material.dart';

class TelaDebito extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Tela Débito'),
      ),
      body: Center(
        child: Text('Conteúdo da Tela Débito'),
      ),
    );
  }
}
